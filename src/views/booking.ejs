<%
/* global fieldsData */
/* global bookingRes */
/* global callTo */
/* global replyTo */
%>
<% if (user) { %>
<script type="text/javascript">
  var NavBarInit = {
    user: {email:"<%= user.email %>", id: <%= user.id %>}
  };
</script>
<% } %>

<div id="onlynavbar" page="booking"></div>

<div class="booking-success">

  <div class="e-ticket confirmation">
    <div class="confirm-code">
      <div class="success ti">Booking</div>
      <div class="wrapper">
        <span class="label-ti">Reservation Code:</span>
        <span class="number"><%= bookingRes.PNR %></span>
      </div>
      <!--- div class="wrapper">
        <span class="label-ti">Ticket: </span>
        <span class="number"><%= bookingRes.ReferenceNumber %></span>
      </div --->

    </div>
  </div>

  <div class="confirm-message">
    <div class="name">Dear&nbsp;<%= fieldsData.FirstName %> <%= fieldsData.LastName %>,</div>
    <div class="thanks">Thank you for choosing Avaea!</div>
    <div class="copy">
      You're all set for your next trip. Your ticket has been issued as an electronic ticket.
      Please check your email for confirmation.
    </div>
  </div>

  <!-- ------------- REACT FLIGHT UNIT ------------ -->
  <!-- -------------------------------------------- -->

  <!-- script for the React flight unit -->

  <script>
  var ItineraryData = <%- JSON.stringify(itineraryData) %>;
  var InitResultData = {
    iconSpriteMap: []
  };
  </script>

  <div class="trip ti">Trip Details</div>

  <div class="flight-unit">

    <!-- here is the div that the React unit replaces  -->
    <div id="booked-flight-unit" class="booked-flight-unit"></div>

  </div>

  <!-- -------------------------------------------- -->
  <!-- --------- ENDS REACT FLIGHT UNIT ----------- -->


  <div class="help-contact">
    <span class="copy">
      <% var _mailto = replyTo.match(/(.*)<(.+)>/); %>
      Need help?&nbsp; <a href='mailto:<%- encodeURIComponent(_mailto[1])+ _mailto[2] %>?subject=<%- encodeURIComponent('Booking Confirmation')%>'>Email Us</a>
      or call&nbsp;<%= callTo %>
    </span>

  </div>

  <!--
  <div class="buttons">
    <button class="big-button" onclick="window.location.href='/order?id=<%= fieldsData.id %>&searchId=<%= fieldsData.searchId %>'">
      Buy More
    </button>
  </div>
   -->

</div>

