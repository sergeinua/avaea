<script>
  var InitData = {
    <% if (user) { %>
    user: {email:"<%= user.email %>", id: <%= user.id %>},
    <% } else { %>
    user: false,
    <% } %>
    page: "<%= (typeof showUnsupportedAlert !='undefined' && showUnsupportedAlert) ? '/unsupported' : page %>",
    defaultSearch: <%- JSON.stringify(defaultSearch) %>
  };
  var serviceClass = <%- JSON.stringify(serviceClass) %>;
</script>
<div id="spa-app"></div>
