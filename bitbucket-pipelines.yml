image: node:5.11.0
pipelines:
  default:
    - step:
        script:
          - echo "This script runs on all branches that don't have any specific pipeline assigned in 'branches'."
  branches:
    DEMO-806*:
      - step:
          script:
            #- npm install
            - pwd
            #- ping kernel.org
            - netstat -anp
            - /sbin/ifconfig
          #  - cd src
          #  - npm install
          #  - make test-unit